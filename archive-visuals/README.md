# Archived visuals

Moved here on 18 August 2026, out of the top-level folder so the GitHub Pages workflow no longer picks them up. The Pages deploy publishes on a `visual_*.html` glob at the top level, so anything left there ships.

These three are superseded and are not part of Piece 2.

| File | Status |
| :-- | :-- |
| `visual_complexity_curve.html` | Superseded by `visual_palate_arc.html`. Carries palate quotes with no entry in `quote_source_records.json`. |
| `visual_education_signal.html` | Built around the WSET enrolment angle, which was cut from Piece 2. Never used. |
| `visual_price_paradox.html` | Superseded by `visual_price_quality.html`. Contains a paraphrased version of the Suckling quote ("Suckling is literally charging people for points") rather than the verbatim wording. |

If any of these is ever revived, it needs a pass against the current standards first: verbatim quotes traced to the record file, sentence-case title, single source footer naming the data provider, and no em dashes or antithesis phrasing.
